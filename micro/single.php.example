<?php
/**
 * Example: Override parent theme's single.php
 * 
 * To use this template:
 * 1. Copy this file to the child theme root directory
 * 2. Rename it (remove .example from the filename)
 * 3. Customize as needed
 *
 * @package Mini_Child
 */

get_header();
?>

<div id="primary" class="content-area">
    <main id="main" class="site-main">

        <?php
        while (have_posts()) :
            the_post();
            
            // Use parent theme's template part or create your own
            get_template_part('template-parts/content', get_post_type());
            
            // Or use child theme template part
            // get_template_part('template-parts/content', 'custom');
            
            // Comments
            if (comments_open() || get_comments_number()) :
                comments_template();
            endif;

        endwhile;
        ?>

    </main>
</div>

<?php
get_sidebar();
get_footer();
